Drop these image files here:
  HeroLight.png    - full-screen hero photo shown in "day" mode
  HeroDark.png     - full-screen hero photo shown in "night" mode (toggled via the sun/moon switch)
  SecondImage.png  - full-screen photo shown right after the hero, as the user scrolls down
  note.png         - a heartfelt handwritten-style note / photo shown near the end, before the family blessings section

Recommended: tall portrait orientation, at least 1080x1920, optimized/compressed for web.
If a file is missing, a placeholder box with instructions shows in its place — nothing breaks.
